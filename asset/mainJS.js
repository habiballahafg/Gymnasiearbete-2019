const submitRegister = document.getElementById('submit-register');
const registerForm = document.getElementById('registeration-form');

submitRegister.addEventListener('click', (e) => {
    e.preventDefault();
}); // end addEventListener Register form